{
  "name": "@gpto/black-box",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/runtime.ts --format iife --global-name PantheraBlackBox --minify --sourcemap",
    "dev": "tsup src/runtime.ts --format iife --global-name PantheraBlackBox --watch",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  }
}
